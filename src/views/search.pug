extends base
include mixins/video

block content 
    form(method="GET")
        input(name="keyword", placeholder="Search by title")
        input(type="submit", value="Hit!")

    div
        each video in videos 
            +video(video)